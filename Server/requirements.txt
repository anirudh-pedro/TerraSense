# TerraSense NER — Backend dependencies
# Compatible-release pins (Python 3.11+). Lock exact versions in CI if desired.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0
httpx>=0.27,<1.0

# --- Database (Phase 1: Neon PostgreSQL + PostGIS) ---
SQLAlchemy>=2.0,<2.1
alembic>=1.13,<2.0
GeoAlchemy2>=0.15,<1.0
psycopg2-binary>=2.9,<3.0

# --- AI Risk Prediction (Model 2: XGBoost + rule fusion + SHAP) ---
numpy>=1.26,<2.3
pandas>=2.0,<3.0
xgboost>=2.0,<3.0
shap>=0.44,<1.0
joblib>=1.3,<2.0
