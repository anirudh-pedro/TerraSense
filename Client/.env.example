# Base URL of the TerraSense FastAPI backend.
# The frontend only ever calls the backend — provider API keys (e.g.
# OpenWeatherMap) live server-side in Server/.env, never here.
VITE_API_BASE_URL=http://localhost:8000/api
